import React from 'react';
import ReactDOM from 'react-dom/client';
import NotepadApp from './components/NotepadApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<NotepadApp />);
